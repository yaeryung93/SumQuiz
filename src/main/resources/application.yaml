spring:
  datasource:
    url: jdbc:mysql://localhost:3306/naverogq?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: prince5406@@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true